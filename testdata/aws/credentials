# LOCAL TEST FIXTURE ONLY. All credentials are intentionally invalid.
# FAKE_* values deliberately do not match the format of real AWS keys.
# Do not replace these placeholders with real AWS credentials.

[default]
aws_access_key_id = FAKE_KEY_DEFAULT
aws_secret_access_key = FAKE_SECRET_DEFAULT

[production]
aws_access_key_id = FAKE_KEY_PRODUCTION
aws_secret_access_key = FAKE_SECRET_PRODUCTION

[staging]
aws_access_key_id = FAKE_KEY_STAGING
aws_secret_access_key = FAKE_SECRET_STAGING

# Credentials-only profile: tests discovery and region fallback from this file.
[dev-only]
region = ap-northeast-1
aws_access_key_id = FAKE_KEY_DEV_ONLY
aws_secret_access_key = FAKE_SECRET_DEV_ONLY
